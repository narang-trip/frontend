spring:
  application:
    name: trip-service
  # MongoDB 추가


  data:
    mongodb:
      host: mongo
      username: root
      password: ssafy
      uri: mongodb://mongo
      port: 27017
      database: narang-trip
      uuid-representation: standard


server:
  port: 8081

springdoc:
  api-docs:
    path: /api/trip/v3/api-docs
  swagger-ui:
    path: /api/trip/swagger-ui.html
